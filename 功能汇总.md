# 实时语音对话项目 —— 功能汇总

> 位置：`D:\DeepSeek-Harness\voicebot`　运行环境：`D:\My-Neuro\lianlian-v0.1\venv_app`（Python 3.12）
> 模型：lianlian（llama.cpp + LoRA，CUDA/Vulkan 后端）· SenseVoiceSmall（识别）· Kokoro-82M-v1.1-zh（合成）· silero（VAD）· bge-base-zh（记忆嵌入）
> 硬件：RTX 5060 Laptop 8GB

---

## 一、核心能力

| 能力 | 说明 |
|---|---|
| **语音对话** | 对着麦克风说话，她识别后回答并用语音回你 |
| **键盘对话** | 随时打字回车也能发消息，和语音**同时可用**、共用一个队列 |
| **插话打断** | 打字随时能打断（即时）；说话打断需 `BARGE_IN=1` **且戴耳机**（外放会让她听见自己）|
| **半双工模式（默认）** | `BARGE_IN=0`：她说话期间自动闭麦，从根本上杜绝自问自答 |
| **回声自愈** | 连续 3 次识别出她自己的碎片 → 自动切半双工并提示 |
| **流式应答** | 大模型一边出字、一边按标点分句、一边合成播放，不用等整段生成 |
| **Live2D 形象** | 口型跟着合成语音动 + 情绪切表情（VTube Studio 插件驱动）；没装 VTS 不影响聊天 |
| **桌宠模式** | VTS 窗口置顶/无边框/隐藏任务栏/鼠标穿透 + 全局热键；退出自动恢复 |
| **自渲染桌宠** | live2d-py + GLFW：逐像素透明、待机动作、LLM 动作标签、情绪反应、说话身体动作、视线跟随、拖拽 |
| **Web 控制台** | 浏览器里管理记忆（改权重/删除）、切换 Live2D、聊天+浏览器语音输入 |
| **模型统一配置** | `models.json` 管所有模型（优先级高于 .env），llama 路径自动同步进 bat |
| **可移植部署** | 路径相对化 + `package_release.py` 打源码包 + `setup_new_machine.bat` 一键装 + `check_env.py` 体检 |
| **人设** | 直接用你 Modelfile 里写的人设；`Modelfile.voice` 可加"语音通话"约束 |
| **上下文** | 保留最近 10 轮对话（`LLM_HISTORY_TURNS`）|
| **可换 TTS** | 三选一：Kokoro v1.1-zh（最快）/ 微软 Edge（最自然）/ **GPT-SoVITS（音色克隆）** |
| **主动性** | 安静久了会自己搭话；早上问候/饭点提醒；没被理会退避；房间没人就不说 |
| **长期记忆** | 跨会话记住你说过的事；档案层永远注入 + 事实/事件层向量检索 Top-8 |
| **自动记忆** | 聊完自动抽事实入库：GBNF 语法约束 + 问句闸门 + 重叠校验（0 误报）；去重时保留信息更全的那条 |
| **全程本地** | 识别/合成/大模型都在本机，不联网、不花钱（启动时校验模型除外） |

---

## 二、实测性能

| 环节 | 模型 | 位置 | 实测 |
|---|---|---|---|
| 说话检测 | silero-vad | CPU | 80% 命中 / 静音 0 误报 |
| 识别 | SenseVoiceSmall | RTX 5060 | 2.5s 语音 → **0.16s** |
| 大模型 | Qwen2.5-7B q4 + LoRA | llama.cpp / Vulkan | 首字 **0.08s**（热态），显存 4.9GB |
| 合成 | Kokoro-82M-**v1.1-zh**（103 个中文音色）| RTX 5060 | 0.29~0.65s/句 |
| **首句出声** | | | **0.29 ~ 0.65 秒** |
| 冷启动 | 三个模型加载+预热 | | 24 ~ 45 秒 |
| 显存占用 | 三者同时驻留 | | **6.6 GB / 8 GB** |

---

## 三、命令行

```bat
run.bat                                  :: 混合模式（语音 + 键盘）
run.bat --text                           :: 只用键盘
run.bat --no-typing                      :: 只用语音
run.bat --devices                        :: 列设备 + 测麦克风
run.bat --wav a.wav --out reply.wav      :: 离线跑一段录音并保存回复语音

python tools\\mic_test.py                       :: 麦克风体检（音量条 / 录音 / 调参建议）
python tools\\selftest.py --play                :: 三段链路自检（合成→识别→大模型）
```

---

## 四、软件架构

```
麦克风 ──VAD 断句──┐
                   ├──→ asyncio 队列 ──→ SenseVoice ──→ llm ──→ gsv ──→ 扬声器
键盘 ──按行读取───┘                                         │
                                                    按标点分句流式合成
```

| 文件 | 职责 |
|---|---|
| `core/voice_chat.py` | 主循环、两种输入合流、打断逻辑、分句流式 |
| `core/audio_io.py` | 采集线程 / VAD 断句 / 播放队列 / 设备诊断 / 自动校准 |
| `core/vad.py` | silero → webrtcvad → 能量法（三级降级）|
| `core/asr.py` | SenseVoice（本地缓存优先）+ OpenAI 兼容接口备选 |
| `core/llm.py` | OpenAI 兼容接口（llama.cpp / Ollama），流式 + 预热 + 注入记忆 |
| `core/memory.py` | 长期记忆：SQLite + bge-base-zh 嵌入 + numpy 检索 |
| `core/tts.py` | Kokoro（去 Markdown / 去旁白 / 掐静音）+ API 备选 |
| `live2d/avatar.py` | Live2D 驱动：播放电平 -> MouthOpen 口型，情绪 -> 表情/热键（VTube Studio 协议）|
| `tools/avatar_test.py` | Live2D 连通性测试（--mock 内置假 VTS，不用装 VTube Studio）|
| `live2d/desktop_pet.py` | 桌宠窗口控制（纯 Win32 API，无额外依赖）|
| `live2d/native_pet.py` | 自渲染桌宠本体（live2d-py + GLFW，跑在 live2d-venv）|
| `live2d/native_pet_link.py` | 主程序侧桥：TCP 推口型/情绪 + 渲染器进程管理 |
| `webui/` | Web 控制台（aiohttp REST + WebSocket + 静态前端）|
| `models.json` / `tools/gen_models.py` / `tools/sync_models.py` | 统一模型配置及生成/同步工具 |
| `webui/catalog.py` | 模型/资源扫描（LLM/TTS/嵌入/ASR/Live2D）|
| `setup_native_pet.bat` | 一键搭建自渲染环境（live2d-venv）|
| `core/config.py` | 50+ 项配置，全部可用 `.env` 覆盖 |
| `tools/mic_test.py` | 麦克风体检工具 |
| `tools/selftest.py` | 分阶段自检 |
| `Modelfile.voice` | 可选的"语音通话版"人设 |
| `_vendor/` | 自带的 sounddevice + silero-vad，**不需要改你的 venv** |

---

## 五、稳健性设计（踩过的坑都变成了自动处理）

| 场景 | 处理 |
|---|---|
| 麦克风权限被拦 / 打不开 | 给出可操作提示（权限路径 + 设备号），**自动降级成键盘模式**继续用 |
| 麦克风电平差异极大 | 开机安静听 1 秒，**按实测底噪自动校准** VAD 门槛 |
| 环境噪声大水（你的情况 -24dBFS）| 自动优先用 **silero 神经网络 VAD** |
| 麦克风信号太轻 | `INPUT_GAIN` 数字放大 |
| 麦克风全是静音 | 底噪 < -95dB 时直接警告"没送声音进来" |
| funasr 每次启动联网校验 | 改为**直接读 modelscope 本地缓存**，离线秒起 |
| 识别出幻觉（"请不吝点赞"等）| `is_noise()` 过滤，不送去大模型 |
| 咳嗽/短促杂音 | `MIN_SPEECH_MS` 丢掉，不触发对话 |
| 她说长句带 `（动作）` 旁白 | TTS 层剥掉不朗读，`Modelfile.voice` 从源头约束 |
| 大模型服务不支持 keep_alive | 首次失败自动关掉该项重试 |
| 音频输出设备打不开 | 提示用 `--devices` 看设备号、改 `OUTPUT_DEVICE` |
| 打字触发打断时把自己那轮取消 | 已修：只有她**正在说话**时打字才触发打断 |

---

## 六、配置速查（`.env`）

**人设/说话**
`LLM_MODEL` `LLM_MAX_TOKENS` `LLM_HISTORY_TURNS` `LLM_KEEP_ALIVE` `LLM_SYSTEM_PROMPT`(留空用 Modelfile)

**识别** `ASR_BACKEND` `ASR_MODEL` `ASR_DEVICE` `ASR_LANGUAGE`
**合成** `TTS_VOICE` `TTS_SPEED` `TTS_DEVICE` `TTS_REPO` `TTS_STRIP_BRACKETS`
**音频** `INPUT_DEVICE` `OUTPUT_DEVICE` `PREFER_WASAPI` `INPUT_GAIN` `SAMPLE_RATE`
**断句** `VAD_BACKEND` `VAD_THRESHOLD` `SILENCE_MS` `MIN_SPEECH_MS` `VAD_AUTO_CALIBRATE`
**交互** `BARGE_IN` `ALLOW_TYPING` `GREETING` `DEBUG_LATENCY`

---

## 七、当前限制

- **无长期记忆**：只带最近 10 轮上下文，不记得"上次聊过的事"
- **形象**：Live2D 口型/表情已支持（VTube Studio 插件驱动，需另装 VTS）
- **无回声消除(AEC)**：外放必须 `BARGE_IN=0`，戴耳机体验最好
- **回复长度靠约束**：基础人设没限制输出长度，建议用 `Modelfile.voice` 重建
- **平台限定 Windows**：设备诊断、venv 路径是为你这台机器写的
- 首次启动依赖本地已缓存的模型（SenseVoice / Kokoro 已在缓存里）

---

## 八、可以继续加

1. ~~**Live2D 口型**~~ ✅ 已实现（见 README「三·十、Live2D 形象」）
2. **长期记忆**：`nomic-embed-text`（你已拉取）+ 向量库，每轮抽取事实
3. **情绪表情**：SenseVoice 输出里的 `<|HAPPY|>` `<|ANGRY|>` 标签现在被剥掉了，可用来驱动表情
4. **Kokoro 真流式**：按 KPipeline 的 yield 边出边播，再省 ~0.1s
5. **唤醒词**：替代"一直在听"，避免误触发

---

*最后更新：麦克风链路打通（silero VAD 接入）之后*
